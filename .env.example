# ReviewLens Environment Configuration

# Backend API (optional - only needed if using backend API instead of browser ML)
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Leave empty to use browser-based sentiment analysis (default)
NEXT_PUBLIC_API_URL=
